import { Space } from 'antd';

const TableHeader = (prefixs: string[], suffixs: string[]) => {
	return (
		<div className='flex-row flex-space-between'>
			<Space>
        
      </Space>
      <Space>a</Space>
		</div>
	);
};

export default TableHeader;
